# Add index

```sql
ALTER TABLE golfscores ADD INDEX IdxScore(score); # add an index (tell MySQL to store all the Scores in a special place (behind the scenes) so that they can be searched extra-fast)
```
